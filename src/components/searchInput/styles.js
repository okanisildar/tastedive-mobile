import { searchInputBackground } from '../../constants/theme'

const styles = {
  searchInput: {
    backgroundColor: searchInputBackground,
    justifyContent: 'center',
    width: '80%',
    minHeight: 30,
    paddingLeft: 10,
  },
  searchInputHover: {
    backgroundColor: 'white',
    justifyContent: 'center',
    width: '80%',
    minHeight: 30,
    paddingLeft: 10,
  },
}

export default styles
